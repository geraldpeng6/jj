# MCP服务器配置
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=8000
MCP_SERVER_PROTOCOL=http

# 注意：在本地开发环境中，我们使用file://协议直接打开HTML文件
# 在服务器环境中，应该配置为服务器的域名或IP地址

# 如果使用HTTPS，设置为https
# MCP_SERVER_PROTOCOL=https
# MCP_SERVER_PORT=443

# MCP服务配置
MCP_HOST=0.0.0.0
MCP_PORT=8000
MCP_WORKERS=2

# 静态文件配置
STATIC_DIR=data/charts
